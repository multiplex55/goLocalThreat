name: frontend-ci

on:
  push:
    paths:
      - 'frontend/**'
      - 'internal/app/app_service.go'
      - 'scripts/check-bindings-freshness.sh'
      - '.github/workflows/frontend-ci.yml'
  pull_request:
    paths:
      - 'frontend/**'
      - 'internal/app/app_service.go'
      - 'scripts/check-bindings-freshness.sh'
      - '.github/workflows/frontend-ci.yml'

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Verify build script exists
        run: node -e "const pkg=require('./package.json'); if(!pkg.scripts?.build){process.exit(1)}"

      - name: TypeScript compile check
        run: npm run typecheck

      - name: Generated bindings contract compile check
        run: npm run test:bindings-contract

      - name: Unit tests
        run: npm run test

      - name: Build frontend
        run: npm run build

      - name: Verify generated bindings are fresh
        working-directory: .
        run: ./scripts/check-bindings-freshness.sh
